---
title: "Titanic"
author: "Aaron Lopez"
date: "8/12/2017"
output: html_document
---

```{r, echo=F}
# Load data and libraries

library(dplyr)
library(tidyr)
library(ggplot2)
library(ggthemes)
library(randomForest)
library(ggtree)

train <- read.csv("train.csv", stringsAsFactors = F)
test <- read.csv("test.csv", stringsAsFactors = F)

full <- bind_rows(train, test)

# Data Wrangling

str(full)
summary(full)
# Age has alot of missing values, so I'll look at replacing those first
ggplot(full, aes(Age, fill=Age)) + geom_histogram(binwidth = 5) + theme_economist()
summary(full$Age)
# We'll replace the NA's with the mean age of 29.88
full$Age[is.na(full$Age)] <- 29.88
# Remove two missing embarked data points 62 and 830
full <- full[-c(62, 830), ]

# Exploratory Analysis

ggplot(full, aes(Pclass, Fare, group=Pclass, fill=Pclass)) + geom_boxplot()
ggplot(full, aes(Embarked, Fare, fill=Embarked)) + geom_boxplot()
# Does where passangeres embarked have an impact on surviving?
table(full$Survived, full$Embarked)
# Most people embarked from S and they also had the most casualties
# Females survived at a m uch higher rate than males
table(full$Survived, full$Sex)
# What about class? 3rd class passangers were least likely to survive
table(full$Survived, full$Pclass)

# Build our model

# split the clean data back into our train and test data sets
full$Embarked <- as.factor(full$Embarked)
full$Sex <- as.factor(full$Sex)
train <- full[1:889,]
test <- full[890:1307,]

# Create Random Forest Model
set.seed(123)

model1 <- randomForest(factor(Survived) ~ Pclass + Sex + Age + SibSp + Fare + Embarked, data=train)
# Model error
plot(model1, ylim=c(0,0.36)) + legend('topright', colnames(model1$err.rate), col=1:3, fill=1:3)
importance(model1)

# Test our model

prediction <- predict(model1, test)
plot(prediction)
solution <- data.frame(PassengerID = test$PassengerId, Survived = prediction, row.names = T)
View(solution)

str```

